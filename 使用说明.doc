<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Pilot Study 学生材料包使用说明</title>
  <style>
    body { font-family: "Microsoft YaHei", Arial, sans-serif; line-height: 1.55; color: #111827; }
    h1 { font-size: 22pt; margin-bottom: 14pt; }
    h2 { font-size: 15pt; margin-top: 18pt; }
    p, li { font-size: 11pt; }
    code { font-family: Consolas, monospace; background: #f3f4f6; padding: 1px 3px; }
    pre { font-family: Consolas, monospace; background: #f6f8fa; padding: 10px; white-space: pre-wrap; border: 1px solid #d0d7de; }
  </style>
</head>
<body>
<h1>Pilot Study 学生材料包使用说明</h1>

<p>这是给学生使用的任务包。里面只包含完成任务需要的材料，不包含研究者访谈、观察、编码或内部研究说明。</p>

<h2>第一步：拿到材料包</h2>
<p>研究者会给你一个下载链接。下载后，请把材料包放到一个容易找到的位置，例如桌面。</p>

<h2>第二步：生成你的任务文件夹</h2>
<p>研究者会给你一个编号，例如 <code>P01</code>。</p>
<p>打开终端，进入这个材料包所在的位置，然后运行：</p>
<pre>bash setup_pilot_project.sh P01</pre>
<p>运行后会在桌面生成一个固定位置的任务文件夹，例如：</p>
<pre>~/Desktop/AIMind_Pilot_Runs/P01_任务文件夹</pre>
<p>如果你不熟悉终端，研究者会帮你完成这一步。</p>

<h2>第三步：在 Codex 中打开任务文件夹</h2>
<p>请在 Codex 中打开刚刚生成的任务文件夹：<code>~/Desktop/AIMind_Pilot_Runs/P01_任务文件夹</code>。</p>
<p>打开后，请先输入：</p>
<pre>你好，我的编号是 P01，现在我要开始今天的任务了。请先阅读当前项目文件夹，告诉我里面有哪些文件。</pre>
<p>请把 <code>P01</code> 换成你的编号。</p>

<h2>第四步：完成任务</h2>
<p>请优先打开这些文件：</p>
<ul>
  <li><code>任务材料/02_学生任务书.doc</code></li>
  <li><code>任务材料/03_用户访谈材料.doc</code></li>
  <li><code>任务材料/04_阶段输出表.xls</code></li>
  <li><code>任务材料/05_Codex项目操作卡.doc</code></li>
</ul>
<p>其中 <code>04_阶段输出表.xls</code> 是你需要填写的文件。</p>
<p>本次任务要完成的是一个适合 5 到 10 分钟体验的网页交互游戏。</p>
<p>你可以直接用自然语言和 Codex 沟通，不需要先学代码。</p>
<p><code>网页原型/</code> 文件夹一开始是空的。制作原型时，请让 Codex 在这个文件夹里创建 <code>index.html</code>，并把最终能打开试玩的网页交互游戏保存在这里。</p>

<h2>第五步：结束任务</h2>
<p>任务结束时，请在 Codex 中输入：</p>
<pre>今天的任务到此为止。请不要再修改文件，请帮我简单总结一下你今天帮我做过哪些事情。</pre>
<p>之后请保留整个任务文件夹。研究者会导出 Codex 对话记录，并自动写入 <code>任务完成检查.log</code>。这个 log 写完后，本次任务才算结束。当前流程不生成 zip 压缩包。</p>
</body>
</html>
